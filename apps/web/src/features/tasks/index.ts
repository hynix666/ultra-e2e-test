// The feature's public surface. src/app imports a feature only through this file, so everything
// else in the folder can change without touching anything outside it.
export { TaskBoard } from "./components/task-board.tsx";
