{
  "name": "@hynix666/ultra-e2e-test",
  "version": "0.0.0",
  "description": "Task status rules for clients of the ultra-e2e-test task API.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hynix666/ultra-e2e-test.git",
    "directory": "packages/ts-library"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.ts\"",
    "check:package": "publint --strict && attw --pack . --profile esm-only",
    "verify": "npm run typecheck && npm test && npm run build && npm run check:package",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.5",
    "@types/node": "^24.13.4",
    "publint": "^0.3.24",
    "typescript": "^7.0.2"
  }
}
