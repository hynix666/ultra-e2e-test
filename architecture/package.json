{
  "name": "architecture",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "likec4 start model",
    "validate": "likec4 validate model",
    "test": "node --test \"test/*.test.mjs\"",
    "build": "likec4 build model --output dist",
    "verify": "npm run validate && npm test && npm run build"
  },
  "devDependencies": {
    "likec4": "^1.59.3"
  }
}
